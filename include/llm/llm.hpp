#pragma once

/**
 * LLM From Scratch - Minimal Tensor + Autograd + Transformer framework
 *
 * Core headers will be added as implementation progresses:
 * - tensor.hpp, autograd.hpp, ops.hpp
 * - nn/*.hpp (Linear, Embedding, LayerNorm, etc.)
 * - optim/*.hpp (SGD, AdamW)
 * - data/*.hpp (Dataset, DataLoader)
 */

namespace llm {

class Tensor;

// Placeholder - will be replaced with actual API
inline const char* version() { return "0.1.0"; }

}  // namespace llm
